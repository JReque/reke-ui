name: PR Lint

on:
  pull_request:
    # Only validate feature PRs targeting develop. Integration PRs
    # (develop -> main) and release-please PRs do not need a conventional
    # title — release-please derives the version from the commits.
    branches: [develop]
    types: [opened, edited, synchronize, reopened]

permissions:
  pull-requests: read

jobs:
  conventional-pr-title:
    runs-on: ubuntu-latest
    steps:
      # Validates the PR title follows Conventional Commits (feat:, fix:, etc.)
      # so release-please can derive the correct version bump. Use squash merge
      # so the validated PR title becomes the commit on the base branch.
      - uses: amannn/action-semantic-pull-request@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
